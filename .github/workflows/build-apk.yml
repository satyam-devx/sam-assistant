name: Build Sam APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install frontend deps
        run: |
          cd frontend
          # Purana kachra saaf karo
          rm -rf node_modules package-lock.json
          # Fresh install with legacy peer deps
          npm install --legacy-peer-deps
          # React-scripts ko explicitly install karo agar missing ho
          npm install react-scripts@5.0.1 --legacy-peer-deps

      - name: Build React app
        run: |
          cd frontend
          # CI=false lagana zaroori hai taaki warnings build na roke
          CI=false npm run build

      - name: Capacitor sync
        run: |
          cd frontend
          npx cap sync android

      - name: Make gradlew executable
        run: chmod +x frontend/android/gradlew

      - name: Build Debug APK
        run: |
          cd frontend/android
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: sam-assistant-debug
          path: frontend/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 7
