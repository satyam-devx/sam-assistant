const express = require('express');
const router = express.Router();

// Placeholder - Phase 2 mein Claude API add karenge
router.post('/', (req, res) => {
  res.json({ message: 'Chat route ready ✅' });
});

module.exports = router;
