const express = require('express');
const router = express.Router();

// Placeholder - Phase 4 mein study tools add karenge
router.get('/', (req, res) => {
  res.json({ message: 'Study route ready ✅' });
});

module.exports = router;
